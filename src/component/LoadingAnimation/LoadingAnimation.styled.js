import { Box, styled } from "@mui/material";

export const LOADINGANIMATED = styled(Box)(({ theme }) => ({
    width: '100%',
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
}));